// {{NAME}} — generated by TERMy ({{LANG}})
// Assumes the slice is already sorted.
package main

import "fmt"

func binarySearch(target int, data []int) (int, bool) {
	low, high := 0, len(data)-1
	for low <= high {
		mid := (low + high) / 2
		switch {
		case data[mid] == target:
			return mid, true
		case data[mid] < target:
			low = mid + 1
		default:
			high = mid - 1
		}
	}
	return -1, false
}

func main() {
	target := {{TARGET}}
	data := []int{ {{ARRAY}} }

	if i, ok := binarySearch(target, data); ok {
		fmt.Printf("Found %d at index %d\n", target, i)
	} else {
		fmt.Printf("%d not found\n", target)
	}
}