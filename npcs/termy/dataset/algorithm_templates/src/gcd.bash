#!/usr/bin/env bash
# {{NAME}} — generated by TERMy ({{LANG}})
set -euo pipefail

gcd() {
    local a="$1" b="$2" t
    while (( b != 0 )); do t=$b; b=$((a % b)); a=$t; done
    printf 'GCD(%s, %s) = %s\n' "$1" "$2" "$a"
}

gcd {{A}} {{B}}