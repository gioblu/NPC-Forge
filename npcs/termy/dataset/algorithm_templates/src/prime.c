/* {{NAME}} — generated by TERMy ({{LANG}}) */
#include <stdio.h>

int main(void) {
    const long n = {{N}};

    if (n < 2) {
        printf("%ld is NOT prime (less than 2)\n", n);
        return 0;
    }
    for (long i = 2; i * i <= n; i++) {
        if (n % i == 0) {
            printf("%ld is NOT prime (divisible by %ld)\n", n, i);
            return 0;
        }
    }
    printf("%ld is PRIME\n", n);
    return 0;
}