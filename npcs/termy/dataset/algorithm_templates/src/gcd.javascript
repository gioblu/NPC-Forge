#!/usr/bin/env node
// {{NAME}} — generated by TERMy ({{LANG}})
"use strict";

/** Euclid's algorithm — iterative. */
function gcd(a, b) {
  while (b !== 0) [a, b] = [b, a % b];
  return a;
}

const a = {{A}};
const b = {{B}};
console.log(`GCD(${a}, ${b}) = ${gcd(a, b)}`);