/* {{NAME}} — generated by TERMy ({{LANG}})
 * {{ARRAY}} is comma-separated and {{ARRAY_LEN}} is its length —
 * both are injected by the generator. */
#include <stdio.h>

int main(void) {
    int arr[] = { {{ARRAY}} };
    const int n = {{ARRAY_LEN}};

    printf("Before:");
    for (int i = 0; i < n; i++) printf(" %d", arr[i]);
    printf("\n");

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                int tmp = arr[j];
                arr[j] = arr[j + 1];
                arr[j + 1] = tmp;
            }
        }
    }

    printf("After: ");
    for (int i = 0; i < n; i++) printf(" %d", arr[i]);
    printf("\n");
    return 0;
}