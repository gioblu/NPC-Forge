/* {{NAME}} — generated by TERMy ({{LANG}}) */
#include <stdio.h>

int main(void) {
    const int n = {{N}};
    long a = 0, b = 1;

    printf("First %d terms of the Fibonacci sequence:\n", n);
    for (int i = 0; i < n; i++) {
        printf("%ld ", a);
        long next = a + b;
        a = b;
        b = next;
    }
    printf("\n");
    return 0;
}