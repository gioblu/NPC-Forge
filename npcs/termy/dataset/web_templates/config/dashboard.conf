# web_templates/config/dashboard.conf
KEYWORDS="dashboard admin panel metrics analytics monitor ops infrastructure app stats"
FALLBACK_TITLE="TERMy OS — Infrastructure Overview"
FALLBACK_DESCRIPTION="Live view of the production fleet — latency, throughput and security events across all regions."