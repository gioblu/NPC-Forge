# web_templates/config/landing.conf
KEYWORDS="landing saas product homepage startup automation pipeline shell deploy tool default"
FALLBACK_TITLE="TERMy — Automate from the terminal"
FALLBACK_DESCRIPTION="A deterministic terminal companion that compiles sites, pipelines and reports straight from the shell — no runtime, no config, no surprises."